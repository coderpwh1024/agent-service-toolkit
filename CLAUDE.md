# CLAUDE.md

## 注释风格

- 默认情况下，不要在新增或编辑的代码中添加注释。
- 只有当“为什么这样做”并不明显时才添加注释，例如存在隐藏约束、微妙的不变量，或针对特定缺陷的变通方案。绝不要解释代码“做了什么”——清晰的命名应该足以说明这一点。
- 切勿编写多段式注释块，也不要在行内过度解释。注释应与周围代码一样简洁。
- 例外：全新文件可以在开头添加一段简短且确实有用的模块级或文件级文档字符串。但这并不意味着可以在文件其余部分加入冗长的行内注释。
- 编辑现有文件时，应沿用该文件已有的注释密度和风格，不要引入比原有风格更繁重的注释方式。

## 维护者脚手架与模板内容

此代码仓库是一个 GitHub 模板。下游项目首次从模板创建时，`.github/workflows/template-cleanup.yml` 会按照约定而非硬编码列表，移除仅供维护者使用的脚手架。请将新的脚手架放在清理流程已经覆盖的位置：

- 维护者自动化内容（技能、钩子、定时运行任务）应放在 `.claude/` 或 `docs/maintenance/` 中——这两个目录会被整体删除，因此会自动纳入清理范围。
- 仅供维护者使用的*工作流*必须包含所有者保护条件 `if: github.repository == 'JoshuaC215/agent-service-toolkit'`；清理流程会删除所有带此保护条件的工作流，因此也会自动纳入清理范围。
- 只有当仅供维护者使用的文件必须位于共享目录（`scripts/` 等）中，且无法包含保护标记时，才应将其添加到 `template-cleanup.yml` 的显式列表中。如果添加此类文件却未更新清理配置，就会导致脚手架泄漏到从模板创建的项目中；这是唯一会发生此问题的情况。

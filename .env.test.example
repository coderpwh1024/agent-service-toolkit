# Copy this file to .env.test and replace the password placeholder locally.
NACOS_USERNAME=nacos
NACOS_REGISTER_SERVICE=false
NACOS_PASSWORD=replace-with-test-nacos-password

# Set this when the test environment must advertise a fixed reachable address.
# NACOS_SERVICE_IP=replace-with-test-service-ip
# NACOS_SERVICE_PORT=8000

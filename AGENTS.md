# AGENTS.md

## 注释风格

- 默认不要在新增或编辑的代码中添加注释。
- 仅当代码背后的*原因*并不明显时才添加注释，例如存在隐含约束、微妙的不变量，或需要规避某个特定错误。绝不要解释代码*做了什么*，清晰的命名应当足以说明这一点。
- 不要编写多段式注释块，也不要在行内过度解释。注释应与周围代码一样简洁。
- 例外情况：全新文件可以在开头添加简短且确有帮助的模块级或文件级文档字符串，但这并不意味着可以在文件其余部分添加冗长的行内注释。
- 编辑现有文件时，应保持其原有的注释密度和风格，不要引入更繁重的注释风格。

## 维护者脚手架与模板内容

本仓库是一个 GitHub 模板。下游仓库首次从模板创建时，`.github/workflows/template-cleanup.yml` 会根据约定（而非硬编码列表）移除仅供维护者使用的脚手架。新增脚手架应放在清理流程已覆盖的位置：

- 维护者自动化内容（技能、钩子、定时任务）应放在 `.claude/` 或 `docs/maintenance/` 中。这些目录会被整体删除，因此会自动纳入清理范围。
- 仅供维护者使用的*工作流*必须包含所有者守卫条件 `if: github.repository == 'JoshuaC215/agent-service-toolkit'`；清理流程会移除所有带有该守卫条件的工作流，因此此类文件也会自动纳入清理范围。
- 只有当仅供维护者使用的文件必须放在共享目录（如 `scripts/`）中，且无法携带守卫标记时，才应将其加入 `template-cleanup.yml` 的显式列表。如果添加此类文件却未更新清理配置，该脚手架就会泄漏到下游仓库中。
